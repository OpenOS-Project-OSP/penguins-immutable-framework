name: Build CI images

# CI images (voidlinux-ci, opensuse-tumbleweed-ci) are owned by and built from
# immutable-linux-framework. This workflow is intentionally a no-op here.

on:
  workflow_dispatch:

jobs:
  noop:
    name: Images built from immutable-linux-framework
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI images are built and published by immutable-linux-framework. Nothing to do here."
